# Tasks

> **What is actually next.** Everything here is open. Deferred and parked work
> is in [BACKLOG.md](BACKLOG.md); reasoning and dead ends are in
> [WORKLOG.md](../WORKLOG.md) (written for the next session, not for Siggie);
> completed rounds are in [archive/](archive/).
>
> Architectural rules and the gotchas to read before running anything are in
> [CLAUDE.md](CLAUDE.md).
>
> **Refer to a task by its tag, never by its row.** Row order is priority and
> changes; the tag is permanent and follows the task into [archive/](archive/)
> when it closes, so `TASKS drop-sibs` in a code comment still resolves after
> the table has been reordered three times. References to "item 2" in older
> comments name the table of their date — resolve them through the archive.
>
> **A subtask is `parent/child`** — `help-finish-authoring/ownership` — written
> as its own row directly under the parent. The slash keeps the parent's tag
> greppable from the child's, which is what the old `1a`/`1b` numbering was
> for, without a number that moves.

---

## Now — finish the tours

The active work. All five tours are WRITTEN and the content tests are green;
what is left is reading them and fixing what reads badly.

**Start with [`read-tours`](#now--finish-the-tours)** — reading the five tours
in the browser and noting what reads badly, which is the only thing the tests
cannot do. Two are new as of 2026-09-17 and have never been read: *Using the
Explorer* and *What BDCHM is built with*. Ownership has been read as far as
`owns-target`. Findings feed
[`help-finish-authoring`](#now--finish-the-tours).

 | Tag               | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Where                                                                          |
 |-------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------|
 | `help-finish-authoring/ownership` | **The Ownership tour — the second half.** Seven steps: `which-way`, `the-legend`, `owns-target`, `belongs-to-target-backward-by-entity`, `belongs-to` (by attribute), `loops`, `rules-recap` (`bar-sides` was cut 2026-09-22). All three rule steps now share one shape, set by `owns-target`: rule name + live counts, beats that characterize the targets, a closing pointer to a Legend dropdown. **Rewritten through `rules-recap` on 2026-09-22; `belongs-to` and `rules-recap` have not been read on screen yet.** The Legend's rule texts were rewritten to match the same day. | [help-content.md](../src/explore/help-content.md) |
 | `help-finish-authoring/oriented` | **SHIPPED 2026-09-17.** Getting oriented became **Using the Explorer**: 12 steps → 8, the three salvaged entries gone (`linkml-context` → its own tour, `selection-tree` merged into the opener, `selection-tree-mechanics` → help-only), and all four `> Salvaged…` notes with them. *Reading the diagram* no longer exists: `rows-and-dots`/`one-edge` folded in, `which-way`/`loops` now OPEN Ownership. The chooser is five tours — BDCHM model, Using the Explorer, **What BDCHM is built with** (new: LinkML, model size, dm-bip), Ownership, Inheritance. Siggie's three `[DECIDE]` answers and the two follow-up decisions are in [WORKLOG](../WORKLOG.md). What is left is `read-tours`. | [help-content.md](../src/explore/help-content.md) |
 | `help-finish-authoring` | **Finish the tour content: review, clean up and improve all five tours, in back-and-forth sessions.** Siggie reads the tours ([`read-tours`](#now--finish-the-tours)); the joint work is the writing. **Audience: researchers first, modelers served by their own tour.** Decided 2026-09-11 as one audience (researchers, detail kept — anyone who needs the weeds, wants to author their own schema or harmonize to this one needs the full explanations); **revised 2026-09-17**, when the orphaned LinkML paragraph became the *What BDCHM is built with* tour rather than being folded into a researcher step. The chooser does NOT label which tour is for whom. See [BACKLOG §why](BACKLOG.md#the-why-argument--two-audiences-one-step). Subtasks below. | [help-content.md](../src/explore/help-content.md) |
 | `read-tours` | **Read the tours in the browser (Siggie).** What the tests cannot see: whether the ringed element is actually on screen at that step (a collapsed panel row, a popover sitting over the thing it describes) and whether the copy reads well. Findings feed `help-finish-authoring`. The pattern that pass established: move the popover off the thing it describes — `Anchor:` the box, `Spotlight:` the row — so the copy and its subject are both on screen. **Ownership is read through `belongs-to-target-backward-by-entity` and `loops` (2026-09-22); `belongs-to` and `rules-recap` are unread.** *What BDCHM is built with* has still never been read. [TOURS_AND_CONTENT.md](TOURS_AND_CONTENT.md) was cut to the category-step recipe and the two out-of-scope decisions; the rest went to WORKLOG. | [help-content.md](../src/explore/help-content.md) |
 | schema-defined-ownership | Let the schema define ownership using `has-part`/`part-of` |  [BACKLOG.md](BACKLOG.md#schema-defined-ownership) |
 | `panel-and-zoom` | **Three faults around the detail panel and the zoom it causes**, found 2026-09-19 while reading a tour; mechanisms confirmed in the code 2026-09-20. They share a symptom, not a cause — fix them separately. **(a) SHIPPED 2026-09-20**, as a side effect of `tour-scalars-back` (now archived): `popStep` restores the previous frame's scalars, so the `detail-panel` step's panel closes on `back` along with every other scalar. ⚠️ **This row had asked for the opposite scoping** — "scope this to `detail`", to avoid rebuilding the per-scalar hybrid rejected on 2026-08-27 ([archive](archive/tasks-2026-08.md)). What shipped is simpler than that hybrid, not a revival of it: one line reading `prev.scalars` wholesale, with the empty stack restoring the state the tour started in. Pinned by `tourStateStack.test.ts`. **(b) Opening the panel auto-fits the canvas into the remaining width, and closing it by hand does not reclaim the space.** The cause is a missing trigger, not a wrong fit: there is **no `ResizeObserver` in the codebase**, and the auto-fit at [OwnershipGraphView.tsx:1537](../src/explore/OwnershipGraphView.tsx) fires only on `[layout, contentW, contentH]` — content-size changes. `zoomToFit` reads `container.clientWidth` live, so a fit WOULD pick up the reclaimed width; nothing asks for one when the container resizes. **(c) Zoom persists after the tour exits** — the guided-tour map comes back in a tiny font. `onTourEnd` ([ExploreApp.tsx:615](../src/explore/ExploreApp.tsx)) restores the selection and nothing else; zoom is outside shareable state by design ([exploreState.ts:16](../src/explore/exploreState.ts) calls it personal preference), and `autoFitRef` is never cleared on exit, so a manual zoom taken during a tour latches. Opening node boxes or starting another tour resets it because those land a new layout. ⚠️ An earlier version of this row guessed (b) and (c) were one bug; they share no mechanism. | [ExploreApp.tsx](../src/explore/ExploreApp.tsx), [OwnershipGraphView.tsx](../src/explore/OwnershipGraphView.tsx), [tourStateStack.ts](../src/explore/tourStateStack.ts), [useZoomPan.ts](../src/explore/graph-core/useZoomPan.ts) |
 | `generalize-explorer` | **Split BDCHM Explorer into a generalized LinkML Explorer.** [sg] *we may be splitting BDCHM Explorer into a new, generalized LinkML Explorer that would work with arbitrary schemas and config settings.* Raised 2026-09-20 while cutting the ownership doc — the question that prompted it was whether a rendering section belongs in a BDCHM doc or a generic one. **The documentation split is part of the task, not a consequence of it**: today every doc mixes the two freely, and the boundary is not obvious. Rough shape of the seam as it already stands — **generic**: the classifier machinery ([ownershipRules.ts](../src/models/ownershipRules.ts)'s `RuleSpec`/`classify`/`OWNERSHIP_VERDICTS`), the DAG and layout, the tour/help engine ([parseHelpContent.ts](../src/help/parseHelpContent.ts), [FORMAT.md](../src/help/FORMAT.md)), the relation bar, the palettes; **BDCHM-specific**: `REFERRED_TO_ENTITIES` and `NAMED_BACK_POINTERS`, [entityCategories.ts](../src/config/entityCategories.ts), every tour's prose, the schema-sync pipeline. ⚠️ Two things that look generic and are not: `SKIP_SUBCLASS_EXPANSION` names `Entity` (a BDCHM class, though 'the universal root' generalizes), and the default-forward rule is itself an editorial judgement another schema might not want. ⚠️ Decide this BEFORE [`ownership-slot-hierarchy`](BACKLOG.md#use-a-has_part--part_of-slot-hierarchy-to-define-ownership-direction) — if ownership direction moves into the schema, the generic app needs no curated sets at all, which changes what the split even looks like. | [BACKLOG.md](BACKLOG.md), [ARCHITECTURE.md](ARCHITECTURE.md) |
 | `style-config` | **Chrome styling belongs in config, and maybe Tailwind should go.** [sg] *the style should have gone into config. i'm wondering if we should just drop tailwind. styles should be controlled by separate configs so they can be easily tweaked*. Raised 2026-09-18 after the relation-bar label was restyled by editing `className` strings inline ([RelationBar.tsx](../src/explore/RelationBar.tsx)) — the change was three utilities deep in JSX, invisible to anyone looking for "what colour is the bar", and it took two rounds to land because there was nowhere to look up what the counts' blue was. **The measurement: 329 hardcoded colour utilities across `src/explore` + `src/help` components, 70 distinct.** ⚠️ **The precedent already exists and was simply not followed** — [appConfig.ts](../src/config/appConfig.ts) centralises the SEMANTIC colours (`RANGE_COLORS`, the P1/P2/P3 scheme) and [helpTheme.css](../src/explore/helpTheme.css) centralises the popover's, with one `em` base so the whole surface moves under one knob. What has no home is the CHROME: box header, relation band, row hover, borders, the dark-mode pair for each. So the narrow task is "give chrome the treatment appConfig/helpTheme already demonstrate", which is worth doing whether or not Tailwind stays. **Dropping Tailwind is a much bigger, separate question** and should not be decided by this complaint: the repo is on Tailwind v4, which reads its config from CSS (`@theme` in [index.css](../src/index.css); `tailwind.config.js` is dead code — see `project_dark_mode_off`), so a `@theme` block of named chrome tokens gets most of the "tweakable from one place" benefit without a rewrite. ⚠️ Decide the narrow one first and see if the broad one still itches. | [RelationBar.tsx](../src/explore/RelationBar.tsx), [OwnershipGraphView.tsx](../src/explore/OwnershipGraphView.tsx), [appConfig.ts](../src/config/appConfig.ts), [index.css](../src/index.css) |
 | `content-problems-in-dev` | **Surface `HelpContent.problems` in the running app, in dev.** [sg] *the misspelling detector does catch it, if i run the help-content tests. Maybe in dev mode the displayed popover could be checked and alert given?* Found 2026-09-18: `Hightlight: ring` on `relation-bar-step` silently dropped the field, so the step dimmed the page and looked like a placement bug — the detector had caught it, but only in `npm test`, and an authoring session reads the tour in the browser rather than running vitest. `parseHelpContent` already collects every unknown field into [`HelpContent.problems`](../src/help/parseHelpContent.ts) and nothing in the app reads it. **The cheap half** is a `import.meta.env.DEV` console.warn at parse time, which costs nothing and catches the whole class. **The visible half** wants a decision: a badge on the popover naming the problems for the ENTRY on screen is what was asked for, and needs `problems` keyed by entry id (today they are one flat `string[]` with the entry name inside the message). ⚠️ Two other silent-degradation classes belong in the same surface if it is built — an anchor that resolved to nothing (normal at runtime, suspicious in a tour step) and a `{{placeholder}}` that resolved to `undefined`. | [parseHelpContent.ts](../src/help/parseHelpContent.ts), [HelpLayer.tsx](../src/help/HelpLayer.tsx) |
 | `tour-position-link` | **A way to copy a link to the tour position you are on.** [sg] *i wonder if stepping through the tour should update the url so people could just copy a url to send someone else to the same spot. or maybe a button. maybe Guided tours dropdown says what step you're currently on if any, with a copy icon*. The LINK FORMAT already exists and needs nothing: `?tour=<slug>&step=<n>`, shipped 2026-09-17 ([FORMAT.md §Linking into a tour](../src/help/FORMAT.md#linking-into-a-tour)). What is missing is a way to GET one without hand-writing it. ⚠️ **The three options in that quote are not variations, they conflict** — decide before building. **(a) Live URL** is the tempting one and fights the existing design: `tour`/`step` are `ONE_SHOT_PARAMS` ([exploreState.ts](../src/explore/exploreState.ts)), deliberately stripped at load so a reload does not restart the tour and so they never ride along into `copy link`. Writing them back on every step reverses both of those on purpose, and then `copy link` — which builds from `buildShareURL` and carries the whole canvas — starts emitting tour links, which may be right or may be surprising. It also makes every step a history entry, so `back` fights the tour's own ← button (compare `tour-scalars-back`). **(b) A button in the popover nav row** is the cheapest and the row is already crowded at the 320px floor (`navMinWidth`, where the dots wrap for exactly this reason). **(c) The chooser showing the current position with a copy icon** is the least invasive — [TourChooser.tsx](../src/explore/TourChooser.tsx) already has `tours`, `tourMeta` and the running tour, `tourIndex`/`positions` reach the step number, and the copy-with-fallback-prompt pattern is in [ExploreApp.tsx](../src/explore/ExploreApp.tsx) (`copy link`) to lift. **(c) looks best and Siggie has not chosen.** Note a link lands on a STEP, never a beat — beat-granularity was declined as very low priority. | [TourChooser.tsx](../src/explore/TourChooser.tsx), [exploreState.ts](../src/explore/exploreState.ts) |
 | `tour-links`      | **Link from tour text to another tour position.** [sg] would be nice to have a way to link from tour text to other tour positions. Syntax undecided: an ordinary markdown link whose target is a location, or `{{link:link text:location}}`, or something else, where location is something like `[tour name or abbr\|step id\|optional beat title]`. Jumping itself exists (`startTour(name, at)`, `goToStep`); what is missing is the authoring syntax, resolving it at parse time so a bad target fails the content test, and rendering it in the popover. **A location vocabulary now half-exists**: `tourSlug`/`tourBySlug` name a tour and `positionOfStep` converts a step number to the position index the jump functions take ([parseHelpContent.ts](../src/help/parseHelpContent.ts), 2026-09-17, built for `?tour=<slug>&step=<n>`). Reuse it rather than inventing a second spelling of "which tour, which step" — and note the URL form deliberately stops at step granularity, while this task's grammar wants a beat too. Shares the location grammar with `beats-in-map`.                                                                                                                                                                                                                                                                                                                                                | [FORMAT.md](../src/help/FORMAT.md)                                             |
 | `schema-comments` | **Surface the schema's `comments` field.** LinkML `comments` is a structured field, not loose yaml — 1054 of them in `bdchm.processed.json`, already parsed by [dataLoader](../src/utils/dataLoader.ts) and already carried on `SlotDefinition`/`EnumDefinition` in [SchemaTypes.ts](../src/models/SchemaTypes.ts). **Enums already use it** ([Element.ts](../src/models/Element.ts), `getExtendedDescription`); slots and classes silently discard it. It holds real content — e.g. `days_supply`: "should be left empty if the source data does not contain a verbatim days_supply, and should not be calculated from other fields" — which is guidance a reader wants and the description does not carry. Render it distinctly from the description, the way [`schema-includes`](#now--finish-the-tours) wants schema prose set off from ours. [sg] | [DetailDrawer.tsx](../src/explore/DetailDrawer.tsx) |
 | `schema-includes` | **Set off `{{model-description:X}}` includes.** [sg] could we have text from `{{model-description:ResearchStudyCollection}}`-type includes set off in some way with title text on the block saying where it came from? or even some kind of link to take you there in the schema? A reader cannot currently tell the schema's own words from ours. The include is expanded in `fillPlaceholders`; the block would need to survive markdown rendering as an element the CSS can style (a `<blockquote>` or a `<div>` with a data attribute), with a title like "from the BDCHM schema" and, if the generated docs URL pattern is stable, a link to that class's page.                                                                                                                                                                                                                                                                                          | [parseHelpContent.ts](../src/help/parseHelpContent.ts)                         |
 | `beats-in-map`    | **Beats in the map, and a beat `Subtitle:` field.** [sg] would like to have a beats expansion in maps: perhaps the beat count badge opens the list of beat titles; which means beat titles become meaningful text. The plan: (a) add a beat `Subtitle:` field; (b) `Subtitle:` and a `#####` heading on the first line of the description are equivalent in display; (c) if `Subtitle:` is missing, the beat title is used as the subtitle; (d) if the field appears and is empty (or `none`?), no subtitle; (e) the map's expansion uses the subtitle, so the beat title can stay a stub. Today every beat description opens with a hand-written `##### Name` heading that duplicates the beat title, which is the thing (b) and (c) would make redundant.                                                                                                                                                                                                   | [TourMap.tsx](../src/help/TourMap.tsx), [FORMAT.md](../src/help/FORMAT.md)     |
 | `elk-layering`    | **Research: what ELK lets us force about layering.** [sg] i know you can't force positioning on ELK, but can you force anything about layering? Start from what `elkLayout.ts` already uses: `elk.partitioning.activate` with a per-node `elk.partitioning.partition` (nodes in a lower partition are laid out in earlier layers, which is the ownership left-to-right), and `elk.layered.considerModelOrder.strategy: NODES_AND_EDGES` (node order within a layer follows input order). Beyond those, ELK's layered algorithm has per-node `elk.layered.layering.layerConstraint` (`FIRST`, `LAST`, `FIRST_SEPARATE`, `LAST_SEPARATE`), the `elk.layered.layering.strategy` choice, and `elk.layered.crossingMinimization.semiInteractive` with `elk.position` to fix in-layer order. The task is to write down which of these the diagram would benefit from, with an example canvas where the current layering is wrong, before changing anything.         | [elkLayout.ts](../src/explore/graph-core/elkLayout.ts)                         |
 | `show-the-change` | **Show the change instead of narrating it.** `Action:` is a past-tense receipt, which reads well when the VIEWER clicked and badly when the tour did it off-screen. Siggie's sequence: anchor the unchecked row → check it → anchor the entity that appeared. Hand-authored as a trial in tour 1 (2026-09-08) at the cost of three beats where there was one; **reverse it if it reads worse**. The feature is deriving that from one beat. Needs design: the popover must reposition BETWEEN phases, which the position model has no notion of, and a half-applied step is a state `back` cannot name. [sg] what i wanted was for one change step to be animated so it's clear to user what's going on. maybe the popover doesn't need to move? stow it on the right somewhere and go to its anchor only when the change is complete. but what the user would probably need to see is the cursor moving to the checkbox (or relation menu or attribute row). | [BACKLOG §Show the change](BACKLOG.md#show-the-change-instead-of-narrating-it) |
 | `address-readout` | **Delete the tour-address readout** once the five tours are written. A `Show content ids` item at the foot of the Help menu, offered only when the host passes `authoringAids`; it makes a popover on screen findable in the content file. Delete `AddressTag`, `showAddresses`/`toggleAddresses`, the `authoringAids` prop and the menu item. ⚠️ **Keep `data-step-address` and `stepAddressOf`** — same popover, looks like the same aid, but [placement.spec.ts](../e2e/placement.spec.ts) navigates by it. | [FORMAT.md](../src/help/FORMAT.md#finding-a-step-you-can-see-on-screen) |
 | `markdown-everywhere` | **Make `OWNERSHIP_RULES` text AUTHORED**, in a markdown file parsed the way [FORMAT.md §Prose inside a field](../src/help/FORMAT.md#prose-inside-a-field) parses help prose, so it gets `:s[…]{color=own-fwd}`, `{{edge:own-fwd}}` and the rest. **Wants a design note first**: it means the rule table stops being the single declaration [ownershipRules.ts](../src/models/ownershipRules.ts) was built to be, which is the thing to argue about before writing it. ⚠️ Do NOT make the legend and example-cases panels into help entries — Siggie's own first thought and immediately rejected as too complicated. **The seam already exists**: [HelpMarkdown.tsx](../src/help/HelpMarkdown.tsx) renders any string the way a tour step's prose renders, and the legend already calls it on `ruleText`, so changing WHERE that string comes from is invisible to the legend. Parts (a) and (c) shipped 2026-09-15 ([archive](archive/tasks-2026-09-15.md)). | [ownershipRules.ts](../src/models/ownershipRules.ts), [HelpMarkdown.tsx](../src/help/HelpMarkdown.tsx) |
 | `variable-box-width` | **Let a box be as wide as its longest row — exploration first, and low priority.** `NODE_W` is a fixed 240px, so 35 of the schema's 87 range names (up to 34 chars) must truncate. [sg] *"it could be, but that's probably a pretty significant fix since the calculated width would need to be fed back to ELK. I don't have a sense of how much width variation there would be... don't explore it now."* **First deliverable is the measurement** — per-class widest-row width, hence how much variation a variable-width canvas would show — not a patch. Full write-up, including why this is NOT the cardinality-truncation bug already fixed, in [BACKLOG §variable-box-width](BACKLOG.md#variable-box-width--let-a-box-be-as-wide-as-its-longest-row). | [OwnershipGraphView.tsx](../src/explore/OwnershipGraphView.tsx), [elkLayout.ts](../src/explore/graph-core/elkLayout.ts) |
 | `subclass-arrowheads` | **Subclass-colored arrowheads do not match their edge color.** Visible wherever a merged box's children are targets: the edge is drawn in the sibling's color but the arrowhead is not, so a bundle of edges into one merged box reads as unrelated lines. Siggie 2026-09-12, on the ObservationSet → Observation family. | [edgeStyle.ts](../src/explore/edgeStyle.ts), [OwnershipGraphView.tsx](../src/explore/OwnershipGraphView.tsx) |
 | `merged-child-relations` | **Hard to see what a merged child is connected to.** [sg] weird that it's so hard to see what SpecimenQuality/SpecimenQuantityObservation are connected to. even when pulling up the `related` menu, you have to scroll to the bottom and there are no color indicators to help. Two problems: a merged child's own relations are buried at the end of the parent's list, and the list carries no sibling color to tie a row back to the band it belongs to — the colors are right there in the box header. | [RelationBar.tsx](../src/explore/RelationBar.tsx), [OwnershipGraphView.tsx](../src/explore/OwnershipGraphView.tsx) |


⚠️ **The menu is the ONLY way into a help-only entry.** The other route was help
mode's `?` hints, and `HELP_MODE_ENABLED` is false — `HelpLayer` renders them
only `if (helpMode && !inTour)`, so a `data-help-id` tag anchors and rings but
opens nothing when clicked. Checked 2026-09-05: `toolbar-siblings` (now
`merged-boxes`, unanchored since the toggle went 2026-09-10),
`relation-bar` and `graph-canvas-reading` were listed and fine; `node-dismiss`
was tagged in `OwnershipGraphView` and reachable from nowhere, and is listed
now. Anything dropped from `HELP_ENTRIES` in `HelpMenu.tsx` is unreachable, not
merely unlisted.

**Editing tour content is safe** — `npx vitest run src/test/helpContent.test.ts`
(~700ms) catches typo'd anchor kinds, un-`Action:`ed state changes, untagged
`help-id` anchors, leftover numeric `Tour:` values and unparseable `State:`
params. Each failure was verified by deliberately breaking the content. A green
run means the content is structurally sound and says nothing about whether the
copy reads well.

⚠️ **What the anchor tests do and do not cover.** Kind and ARGUMENT are both
checked against the live schema and the category config, so `entity-row:Participnt`
fails the build rather than degrading to an unringed popover. Since 8a
([`helpAnchors.test.tsx`](../src/test/helpAnchors.test.tsx)) every DIAGRAM anchor
is also run against the tags its own step's `Change:` would emit — which is how
three live `node-box:` anchors on merged children were caught. **The remaining gap
needs the browser**, and is two things: a step with NO `Change:` inherits whatever
selection is on screen, so there is no canvas to check it against; and whether an
element is actually in the DOM at that moment (a collapsed row, a panel in tree
mode) resolves to nothing for reasons no test can distinguish from a real bug.

---

## Next — correctness, in priority order

| Tag | Task | Est. | Where |
|---|---|---|---|
| `elk-worker` | **Stop killing a warm ELK worker.** `useGraphLayout`'s effect cleanup calls `engine.cancel()` on every spec change whether or not a run is in flight, so the worker is torn down and every click pays worker startup. Terminate only when a run is actually pending. The measurement that found this (2026-09-09, 347 runs: p50 153ms, p90 322ms, floor ~100ms even for 2-node graphs, **every run cold**) likely settles `SPINNER_DELAY_MS` at its current 200 — 26% of runs exceeded it then, almost none should once warm. ⚠️ **The instrumentation is gone** (deleted 2026-09-19 with `elkTimingPlugin.ts`, `elkTiming.ts` and the `recordElkTiming` call; it is in git history if wanted). So this fix would ship **unmeasured**, and the 26% figure is from the old data, not a fresh run. ⚠️ Every number here is from Siggie's machine. [sg] *"maybe we need to test someday when people with slower computers use the app. i don't know."* — parked, not scheduled. | 30 min | [useGraphLayout.ts](../src/explore/graph-core/useGraphLayout.ts) |
| `nav-guards` | **The tour's navigation guards fail silently.** `goTo`, `goToStep` and `startTour` each bail with a bare `return`. ⚠️ **No longer hypothetical** — this swallowed BOTH tour-map dead-click bugs on 2026-09-08 (`d0519dc`); each took far longer to find than it should have, and a dead UI is what Siggie reported as *"weird state"*. Both root causes are fixed; the silence is not. **Loud is the right call** — a `console.warn` per bail naming the guard and what it was asked for, not the reverted deferral machinery. | 15 min | [BACKLOG §goTo](BACKLOG.md#gotos-silent-no-op) |
| `drop-sibs` | **Drop `sibs=0`.** Siggie: *"i never use it anyway and it really crowds the canvas."* The anchor vocabulary does not need it — `child-header:` and `node-box:` name different things in either mode. **The toolbar button and the unmerged render path went 2026-09-10** (siblings always merge now); what is left is plumbing: the `sibs` URL param and `DEFAULTS`/`toQuery`, `LS_KEYS.sibs`, the tour state stack scalar, `ExploreApp`'s `mergeSibs` state and the inert props on `OwnershipGraphView`, FORMAT.md's param table, and the tests that round-trip the param. | ? | [BACKLOG §Drop sibs=0](BACKLOG.md#drop-sibs0) |
| `hover-dims-spotlight` | **Hover washes out the spotlit boxes too.** Siggie, 2026-09-17 (img3): during a tour, hovering a box dims everything else *including the elements the step is ringing*, so the spotlight's own targets grey out and the ring surrounds a washed-out box. Separate from the redraw fix of the same day — that one stopped hover ENGAGING on a relayout; this is hover engaging normally and not knowing the tour is pointing at something. `applyHover` in [OwnershipGraphView.tsx](../src/explore/OwnershipGraphView.tsx) dims by `data-node-id` and has no reference to `data-help-spotlight`, which `HelpLayer` writes on each spotlit element. Cheapest fix is probably to spare anything carrying that attribute (treat it as lit), but check it against `Highlight: dim`, where dimming is the point. Deferred by Siggie: *"don't worry about it now."* | ? | [OwnershipGraphView.tsx](../src/explore/OwnershipGraphView.tsx), [HelpLayer.tsx](../src/help/HelpLayer.tsx) |
| `hover-dim-config` | **Hover dim amounts are inline constants.** Siggie's own note sits at the call: `// [sg] changed this...needs to live in config`. `0.25` nodes, `0.38` edges, `0.08` arrowheads, beside timings that DO live in [anim.ts](../src/explore/graph-core/anim.ts). | small | [OwnershipGraphView.tsx](../src/explore/OwnershipGraphView.tsx) |
| `edge-crossings` | **Edge crossings.** *"A lot of unnecessary edge crossings."* Cause **unmeasured** — do not speculate. Layout is `useGraphLayout`. | ? | — |
| `dead-relation-groups` | **Dead code: `buildRelationGroups`.** It feeds `countsOf` for `NodeVM`'s `relatedCount`/`shownCount`, and **nothing renders those any more** — the relation bar counts its own rows. A second layer of dead code inside a live file. | small | [BACKLOG §Dead code](BACKLOG.md#dead-code-left-by-the-relation-bar) |
| `map-jump-canvas` | **Jumping to a step from the map does not always clear the canvas.** Siggie, 2026-09-10, intermittent and withdrawn for now: "sometimes it does". Unreproduced; nothing changed. Suspects and the first probe to write are in the backlog entry. | ? | [BACKLOG §map jump](BACKLOG.md#jumping-to-a-step-from-the-map-does-not-always-clear-the-canvas) |

⚠️ **`edge-crossings` is an investigation wearing a quick win's clothes.** Its cause is
unmeasured. The standing rule is measure before proposing one.

---

## Then — cleanups with a known shape

  | Tag | Task                                                                                                                                                                                                                                                            | Where                                                                                                                        |
  |----|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------|
  | `palettes` | **Palettes that only half-exist.** Every element-type color is defined **twice** — a palette `hex` and eleven hand-picked Tailwind literals — in two forms that cannot be derived from one another. Changing `RANGE_COLORS` moves the SVG hex and nothing else. | [BACKLOG §Palettes](BACKLOG.md#palettes-that-only-half-exist)                                                                |
  | `inheritance-accessor` | **One inheritance accessor, with a required argument.** Inheritance is derived two independent ways today and neither calls the other. Decided 2026-08-24.                                                                                                      | [BACKLOG §One inheritance accessor](BACKLOG.md#one-inheritance-accessor-with-a-required-argument) |
  | `lint-baseline` | **The lint baseline is wrong, and one real bug hides in it.** [CLAUDE.md](CLAUDE.md) says 20 pre-existing errors; `npm run lint` reports 30, which makes the "compare against the baseline" instruction unusable. Two causes worth separating: **(a)** `.vite/deps/` build artifacts are being linted — add an ignore; **(b)** [`Section.tsx`](../src/components/Section.tsx) calls `useExpansionState` conditionally (`react-hooks/rules-of-hooks`), a genuine latent bug — hook order changes if `expansionKey` ever varies for a mounted Section. Fix (b), ignore (a), then re-record the real number. | [eslint.config.js](../eslint.config.js) |
  | `ownership-slot-hierarchy` | **Move ownership direction into the schema as a `has_part` / `part_of` slot hierarchy.** Siggie asked (2026-09-17) whether LinkML can state ownership direction so the classifier is not needed. It cannot derive it, but it can carry it: two abstract slots (`has_part` → `own-fwd`, `part_of` → `own-bkwd`, BFO URIs) and `is_a` on every class-ranged attribute. Verified on a patched copy with linkml 1.11.1: SchemaView resolves it on inline `attributes:`, lint is clean, `gen-doc` shows it on the slot page only. Steps: **(a)** decide what an unmarked slot means (recommendation: unclassified, flagged by the audit, app still defaults forward); **(b)** an overlay YAML keyed `Class.slot` in this repo, seeded from `REFERRED_TO_ENTITIES` + `NAMED_BACK_POINTERS`, merged by `transform_schema.py` so `processed.json` carries a per-attribute ownership field; **(c)** `OWNERSHIP_RULES` becomes a lookup on that field and the two exception sets are deleted — `child-following-parent` stays; **(d)** `audit_schema_sync.py` flags class-ranged slots under neither parent; **(e)** upstream PR, same route as `in-subset-categories`, after which the overlay no-ops. ⚠️ Upstream's generated class pages would still not show direction without a custom `gen-doc` template — separate, optional. [sg] | [BACKLOG §Let the schema say it](BACKLOG.md#use-a-has_part--part_of-slot-hierarchy-to-define-ownership-direction), [ownershipRules.ts](../src/models/ownershipRules.ts), [transform_schema.py](../scripts/transform_schema.py) |
  | `in-subset-categories` | **Move the category specs into LinkML `in_subset`.** ⚠️ **Blocked: needs an upstream PR approved first** — `in_subset` does not exist in the schema today. Then the categories arrive with the sync instead of rotting here. Membership migrates; `label`, order and `pins` (explanatory, not structural) cannot and stay ours. [sg] | [BACKLOG §in_subset](BACKLOG.md#move-the-category-specs-into-linkml-in_subset) |
  | `enum-detail` | **Enum badges in the Explore drawer are dead, and Nested Tabular's detail never came across.** Purple range badges render as inert `<span>`s by construction. Two options in the backlog: (a) make the badge open the values card, (b) decide what an enum's detail *is* in this drawer first. Scope is everything Nested Tabular exposes, not enums alone. [sg] | [BACKLOG §Enum detail](BACKLOG.md#enum-detail-in-the-explore-drawer) |
  | `intros` | **Improve the intros to README and the first tour** — they might share text. README's *Model shape* does not actually describe the model's shape; it describes how the app adjusts it for comprehension. [sg]                                                | [README](../README.md) · [help-content.md](../src/explore/help-content.md) |
  | `md-includes` | Consider using https://github.com/cmacmackin/markdown-include or https://github.com/zimbatm/mdsh to allow shared includes                                                                                                                                       |                                                                                                                             |

---

## Waiting on Siggie

- **Should the parent's own edge in a fan render black?** Specified black; ships
  in the shared channel colour. [BACKLOG §Edge fan](BACKLOG.md#the-fan-from-observationsetobservations)
- **Obstacle-aware routing for dragged nodes** — not scoped, needs a go-ahead.
  [BACKLOG §Dragging](BACKLOG.md#dragging-is-unfinished)
- [BACKLOG §dag-browser](BACKLOG.md#dag-browser) -- dag-browser currently disabled.
  Too much work. BUT, using it in this app does a great job showing what's broken
  about it that needs fixing. So this should be added as a task for the dag-browser
  project, but when ready to work on it, the work might usefully be performed here.
  This is no longer 'waiting on siggie' but the task of adding it as a task in dag-browser
  is itself a task.

---

## Explicitly not now

Chip-strip redesign · panel resizing/detaching · the ownership legend rebuild ·
CURIE links · the bare diagonal · dragging polish · example-cases restructuring
· extracting `packages/tour-help/` · bringing help mode back.

All keep their full write-ups in [BACKLOG.md](BACKLOG.md) — nothing is lost.
