# Archived tasks — cut from docs/TASKS.md on 2026-09-15

Rows keep their final text. What is still TRUE of the code they describe is in
the live docs — for this one, [OWNERSHIP_CLASSIFICATION.md](../OWNERSHIP_CLASSIFICATION.md)
§Rule 3. The reasoning, including the part where the row's own diagnosis turned
out to be wrong, is in WORKLOG.md (2026-09-15).

---

## `induced-clutter` — DONE 2026-09-15 (`bc9cb50`)

Induced edges are out of the relation bar, the legend and the Ownership tour;
they serve layout only.

⚠️ **Two different mechanisms are both called "induced" — keep them straight.**

(i) `inducedFrom` (ours, `containmentGraph.ts`) retargets a forward-owned edge
at each subclass of its RANGE. Nothing declares the slot, so it is an inference,
not a declaration; filtered in `collectRelations`.

(ii) `inherited_from` (LinkML's `SchemaView.induced_class()`, `induced_schema.py`)
copies a parent's slot onto each subclass of the DECLARING class, so all of them
genuinely hold it; a merged box unions its members and repeats the row once per
member, which `buildRelationRows` collapses. **That collapse is keyed on the
ROWS, not on `inheritedFrom`** — a subclass may re-declare a slot via
`slot_usage` with a narrower range (`DimensionalObservationSet.observations →
DimensionalObservation`), and that row is a different fact that must survive.

Three tests in `src/test/relationBar.test.ts` pin both, plus the re-declare case
that distinguishes them.

**Note on the original row:** it named `inducedFrom` as the cause of
ObservationSet's "4 distinct entities through 13 attributes" and prescribed one
filter in `RelationBar.tsx`. Both were wrong — not one of those 13 rows was an
induced edge, and `RelationEntry` carries no `inducedFrom` to filter on. The row
had been written from a screenshot rather than from the data.

---

## `legend-list-orientation` — DONE 2026-09-15 (`7802bee`…`ee275da`)

The legend's lists were grouped by `p.range` in every section, which lands on
opposite ROLES depending on the rule. Every section now groups
**owner → attribute → owned**, as four pivots over one set of pairs, rendered
as a column-aligned table with a header.

What is still read from: [LEGEND_ORIENTATION.md](../LEGEND_ORIENTATION.md) —
cut to the pivot spec `SHAPES` transcribes, plus the wording decision that is
NOT yet acted on. The design argument and the six review rounds are in
WORKLOG.md (2026-09-14 and 2026-09-15).

Siggie's two eyeball questions came back: the dropped rule indent reads fine,
and the count labels survived the column work.

## `markdown-everywhere` (a) and (c) — DONE 2026-09-15 (`d1ab32e`)

(a) The help system's markdown pipeline is callable on any string by any
component: [HelpMarkdown.tsx](../../src/help/HelpMarkdown.tsx), with the parts
in `markdownParts.tsx` so the component file stays hot-reloadable. (c)
`{{ownership-count:<key>}}` over `DataService.getOwnershipCounts()`, so prose
quotes live numbers.

**(b) is NOT done** and keeps its own row in TASKS.md: making
`OWNERSHIP_RULES[].text` authored markdown rather than a string literal.

Fixed on the way past: `remark-directive` was swallowing the argument of every
unresolved `{{kind:arg}}` placeholder, so schema drift named the kind but not
the missing thing. Not legend-specific — every tour placeholder had it.

