# Archived tasks — cut from docs/TASKS.md on 2026-09-20

Rows keep their final text. What is still TRUE is in the live docs:
[OWNERSHIP_CLASSIFICATION.md](../OWNERSHIP_CLASSIFICATION.md) for what the
rules mean, [ARCHITECTURE.md §How the diagram is drawn](../ARCHITECTURE.md#how-the-diagram-is-drawn)
for how they are rendered. The reasoning — the four keep/cut decisions, why the
phrasing table was 5/7 dead, and the part where the row's own description of the
tour turned out to be wrong — is in WORKLOG.md (2026-09-20).

---

## `ownership-doc-rewrite` — DONE 2026-09-20 (`63056ac`)

**The doc cut.** [OWNERSHIP_CLASSIFICATION.md](../OWNERSHIP_CLASSIFICATION.md)
went from 1096 lines to under 300. Rendering (the color system, edge geometry,
the relation bar, layering and cycles) moved to
[ARCHITECTURE.md](../ARCHITECTURE.md); the `has_part`/`part_of` proposal, the
inheritance-accessor plan and the `any_of` alternatives moved to
[BACKLOG.md](../BACKLOG.md); the rest was cut. `OWNERSHIP_DOC_CUT.md`, the plan
file, is deleted as it instructed.

⚠️ **The row's claim that the tour needed rewriting was false when it was
written.** It said the rule steps "teach three numbered rules that no longer
exist." They do not, and had not for some time — every step was already named,
every count already resolved through an `ownership-count` placeholder, and the
content tests were green. Reading the tour on screen is real work, but it is
`help-finish-authoring/ownership`, not this task, which is where it went when
this row closed.

Rules are NOT numbered, and counts are re-measured rather than hand-adjusted —
`getOwnershipPairGroups` is the source and the legend prints them live. Both
still hold.

### The row as it closed

 | Tag | Task | Where |
 |---|---|---|
| `ownership-doc-rewrite` | **Rewrite the ownership docs and the Ownership tour for the named-rule scheme.** The classifier, the legend and the tests say: an attribute **owns target / forward arrow**, except **belongs to target / by entity** (referred-to entities, keyed by range) and **belongs to target / by attribute** (named back-pointers, keyed `Class.slot`). Induced edges are NOT a slot rule and are shown nowhere; they serve layout only. **Rules are NOT numbered.** ⚠️ Counts are re-measured, never hand-adjusted — `getOwnershipPairGroups` is the source and the legend prints them live. **(i) live counts, (ii) the `referred to` rewording and (b) the doc cut have all SHIPPED** — the tour resolves every count through an `ownership-count` placeholder, and [OWNERSHIP_CLASSIFICATION.md](../OWNERSHIP_CLASSIFICATION.md) went 1096 → 270 lines on 2026-09-20 (rendering → [ARCHITECTURE.md §How the diagram is drawn](../ARCHITECTURE.md#how-the-diagram-is-drawn), `has_part`/`part_of` + inheritance-accessor + `any_of` alternatives → [BACKLOG.md](../BACKLOG.md), the rest cut). **One piece left: (a)** Siggie has still not read the rule steps in the browser. | [help-content.md](../../src/explore/help-content.md) |
