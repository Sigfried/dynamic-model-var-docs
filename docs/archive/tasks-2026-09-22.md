# Archived tasks — cut from docs/TASKS.md on 2026-09-22

Rows keep their final text, with links re-pathed for this directory. The
reasoning, including the reverted agent round on the legend table, is in
WORKLOG.md (2026-09-22).

---

## `panel-refit` — DONE 2026-09-22 (`b341f35`)

`panel-refit`

The deferred question at the end now lives in
[BACKLOG §Overlays](../BACKLOG.md#overlays-one-model-draggable-and-resizable).

## `legend-table-grouping` — DONE 2026-09-22 (`f5b2f86`, `0e691bb`)

**The pivot tables did not read as ownership.** Siggie, 2026-09-22: *"it's not
obvious that the sources own the targets, partly because it's not obvious what
rows go with what."* All four wants shipped, in
[legendTable.css](../../src/explore/legendTable.css) and
[OwnershipLegend.tsx](../../src/explore/OwnershipLegend.tsx):
**(1)** a hairline under each `owns: owned` group, with its chevron back in the
left gutter; **(2)** the `owns: owned` arrows share one column, because the
label row is a subgrid (Claude Design's fix); **(3)** the header arrow sits
inline in that column in every pivot; **(4)** a gap track holds the arrow
column well right of the sources, with the targets right-aligned.
