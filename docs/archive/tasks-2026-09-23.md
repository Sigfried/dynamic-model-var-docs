# Archived tasks — cut from docs/TASKS.md on 2026-09-23

Rows keep their final text, with links re-pathed for this directory. The
reasoning is in WORKLOG.md (2026-09-22).

---

## `help-finish-authoring/ownership` — DONE 2026-09-23 (`e45d52c`, `7059436`)

**The Ownership tour — the second half.** Seven steps: `which-way`, `the-legend`, `owns-target`, `belongs-to-target-backward-by-entity`, `belongs-to` (by attribute), `loops`, `rules-recap` (`bar-sides` was cut 2026-09-22). All three rule steps now share one shape, set by `owns-target`: rule name + live counts, beats that characterize the targets, a closing pointer to a Legend dropdown. **Rewritten through `rules-recap` on 2026-09-22; `belongs-to` and `rules-recap` have not been read on screen yet.** The Legend's rule texts were rewritten to match the same day.
